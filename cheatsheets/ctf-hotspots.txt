Spots
    Win:
    ipconfig /displaydns
    SAM database
    set
    %PATH
    net user, localgroup, administrators
    dir /b /s
    reg
        HKLM\SAM
        HKLM\Security
    
    Linux:
    netstat -name
    arp -a
    ss -t
    /etc/passwd
    /etc/shadow
    /etc/group
    ~ 
    .bash_history
    pf
    firewall
    /etc/sudoers
    sudo -l -l
    find / 
    GREP -Inri -e passw -e secret -e key /etc
    find / -perm 4000
    
